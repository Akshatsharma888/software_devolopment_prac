// use of click events in react. it is an interaction when a user clicks on a specific element we can respond to click by passing an callback to an onclick event handler.

import ClickButton from "./Click.jsx";
function App6(){
  return(
    <ClickButton/>
  );
};

export default App6;